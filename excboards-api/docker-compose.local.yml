services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: excboards_dev
      POSTGRES_USER: excboards
      POSTGRES_PASSWORD: devpassword
    ports:
      - "5432:5432"
    volumes:
      - excboards_pg_data:/var/lib/postgresql/data

volumes:
  excboards_pg_data:
